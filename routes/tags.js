import express from "express";
const router = express.Router();
import fs from "fs";
import crypto from 'crypto';

router.get("/",(_req, res) =>{
    // res.send("made it to get tags!");
    
});
export default router;